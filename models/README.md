# Models

This directory is for model architecture documentation, manifests, configuration, evaluation notes, adapters, and integration contracts only.

## Do not commit

- foundation-model weights
- checkpoints
- generated song binaries
- private datasets or training corpora
- API secrets or credentials

Model artifacts should live in approved external object/model storage and be referenced by versioned manifests.
